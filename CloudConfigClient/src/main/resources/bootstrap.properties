################################
# config-server connectivity configuration
################################
spring.cloud.config.username=${configUserName}
spring.cloud.config.password=${configPassword}
spring.cloud.config.uri=${configURI}
spring.profiles.active=${configProfile}

encrypt.key=${configEncKey}